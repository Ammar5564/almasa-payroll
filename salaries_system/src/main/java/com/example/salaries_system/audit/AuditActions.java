package com.example.salaries_system.audit;

/** Stable codes stored in audit_logs.action */
public final class AuditActions {

    private AuditActions() {}

    public static final String ATTENDANCE_REGISTERED = "ATTENDANCE_REGISTERED";
    public static final String ATTENDANCE_DUPLICATE_REJECTED = "ATTENDANCE_DUPLICATE_REJECTED";
}
